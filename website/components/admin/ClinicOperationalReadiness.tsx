import Link from "next/link";
import type { ClinicOperationalReadiness } from "@/lib/clinic/operational-readiness";

export default function ClinicOperationalReadiness({ readiness }: { readiness: ClinicOperationalReadiness }) {
  return <section className="rounded-2xl border bg-background p-5 sm:p-6"><div className="flex flex-wrap items-baseline justify-between gap-2"><div><h2 className="text-xl font-semibold">AI Booking Setup</h2><p className="mt-1 text-sm text-muted-foreground">Complete authoritative clinic configuration before enabling AI availability.</p></div><p className={`text-2xl font-bold ${readiness.h3Ready ? "text-emerald-700" : "text-amber-700"}`}>{readiness.percentage}%</p></div>{!readiness.policyAvailable && <p role="alert" className="mt-4 rounded-lg border border-amber-200 bg-amber-50 p-3 text-sm text-amber-800">Booking policy columns are unavailable. Apply the approved booking-policy migration before configuring this clinic.</p>}<ul className="mt-5 grid gap-2 sm:grid-cols-2">{readiness.items.map((entry) => <li key={entry.key} className="flex items-center justify-between gap-3 rounded-lg border p-3 text-sm"><span className={entry.ready ? "text-emerald-700" : "text-slate-700"}>{entry.ready ? "✓" : "○"} {entry.label}</span>{!entry.ready && <Link href={entry.actionHref} className="min-h-11 rounded-lg px-3 py-2 font-medium text-blue-700 hover:bg-blue-50">Configure</Link>}</li>)}</ul><p className="mt-4 text-sm font-medium">{readiness.h3Ready ? "AI Booking Ready" : `${readiness.readyCount} of ${readiness.requiredCount} required checks complete`}</p></section>;
}
